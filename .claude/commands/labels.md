Look at the current IDE selection. In that range of the open file:
- Numbered display equations: append a label on the same line after the closing $  e.g. $ F = m a $ <eq:newtons-second>
- Figure captions: append a label after the caption text  e.g. <fig:drag-coefficient>
- Where surrounding text says "equation (3)", "Eq. 3", etc., replace with @eq:slug using the label you assigned.
- Use lowercase-hyphenated slugs derived from the equation's physical meaning.
- Do NOT label inline equations.
- Do NOT change anything else.

Edit the file directly at the selected lines.
